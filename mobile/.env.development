# Dev no browser (`npm run dev`): base relativa para usar o proxy do Vite
# (server-side, sem CORS no browser). O proxy aponta para produção (MVP).
VITE_API_BASE_URL=
VITE_API_PROXY_TARGET=https://app.imedto.com
