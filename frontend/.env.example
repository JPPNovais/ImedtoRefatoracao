# URL base da API em produção (em dev o Vite proxy redireciona /api para localhost:5000)
VITE_API_BASE_URL=https://sua-api.com

# As chaves do Supabase ficam SOMENTE no backend (appsettings.json ou variáveis de ambiente do servidor).
# O frontend não tem acesso a nenhuma chave do Supabase — este é o padrão BFF.
